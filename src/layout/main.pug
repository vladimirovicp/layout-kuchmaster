doctype html
block variables_layout
block vars

include ../component/variable

html(lang= 'ru')
    head
        meta(charset='utf-8')
        meta(name="viewport", content="width=device-width,initial-scale=1")
        title #{title}
    body

        include ../component/header
        block content
        include ../component/footer

        .pug-download-svg(style="display:none;")
            img(src=require('/img/svg/sprite.svg'), alt="")
